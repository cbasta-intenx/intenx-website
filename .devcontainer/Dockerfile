# Use the official Node 20 LTS image as the base
FROM mcr.microsoft.com/devcontainers/javascript-node:20-bookworm

# **[Optional]** Install any global dependencies the agent might need later
# Since we are using Next.js, we don't need a lot globally, but useful for agents:
# RUN npm install -g pnpm # Example of adding a tool

# Set the working directory (optional, but good practice)
WORKDIR /workspace